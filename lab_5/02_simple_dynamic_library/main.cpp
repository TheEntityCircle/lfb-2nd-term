#include <iostream>
#include "goodbye.h"
#include "anatoly.h"
#include "cherdenko.h"

using std::cout;
using std::endl;

int main()
{
    anatoly(3);
    goodbye(1);
    anatoly(1);
    cherdenko(1);
    return 0;
}
